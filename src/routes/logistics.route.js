import{Router}from"express";import{LogisticsController as c}from"../controllers/logistics.controller.js";import{requiresAuth,requiresPermission}from"../middlewares.js";const r=Router();r.use(requiresAuth);const read=requiresPermission("logistics","read"),create=requiresPermission("logistics","create"),update=requiresPermission("logistics","update"),del=requiresPermission("logistics","delete");r.get("/masters",read,c.masters);r.get("/settings",read,c.settings);r.put("/settings",update,c.updateSettings);r.get("/lanes",read,c.lanes);r.post("/lanes",create,c.createLane);r.delete("/lanes/:id",del,c.deleteLane);r.get("/freight-engine/:laneId",read,c.engine);r.get("/validate-freight",read,c.validate);r.get("/transporters",read,c.transporters);r.post("/transporters",create,c.createTransporter);r.get("/transporters/:id",read,c.transporter);r.delete("/transporters/:id",del,c.deleteTransporter);r.post("/quotes",create,c.addQuote);r.get("/shipments",read,c.shipments);r.post("/shipments",create,c.addShipment);r.get("/cost-register",read,c.costs);r.post("/cost-register",create,c.addCost);r.get("/analytics",read,c.analytics);export default r;
