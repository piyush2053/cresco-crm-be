import{Router}from"express";import{NotificationsController as c}from"../controllers/notifications.controller.js";import{requiresAuth}from"../middlewares.js";const r=Router();r.use(requiresAuth);r.get("/",c.list);r.get("/history",c.history);r.put("/read-all",c.readAll);r.put("/clear",c.clear);r.put("/:id/read",c.read);export default r;
