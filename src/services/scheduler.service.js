import cron from"node-cron";import{ReportsService}from"./reports.service.js";import{NotificationsService}from"./notifications.service.js";import{query}from"../db.js";
let started=false;async function runWeekly(){const run=(await query("INSERT INTO scheduled_job_runs(job_name,status) VALUES('weekly_business_report','running') RETURNING id")).rows[0];try{await ReportsService.createWeeklyReport();await query("UPDATE scheduled_job_runs SET status='completed',completed_at=now() WHERE id=$1",[run.id]);await NotificationsService.notifyAdmins("Weekly report sent","Buyer, Supplier and Order Excel report was emailed to all active admins.","success","/reports")}catch(e){await query("UPDATE scheduled_job_runs SET status='failed',details=$2,completed_at=now() WHERE id=$1",[run.id,e.message]);console.error("Weekly report failed:",e.message)}}
async function operationalAlerts(){try{const prices=+(await query("SELECT count(*) FROM supplier_grade_prices WHERE is_active AND expires_at BETWEEN now() AND now()+interval '12 hours'")).rows[0].count,overdue=+(await query("SELECT count(*) FROM finance WHERE payment_status<>'Paid' AND due_date<CURRENT_DATE")).rows[0].count,deliveries=+(await query("SELECT count(*) FROM orders WHERE deleted_at IS NULL AND status NOT IN('Completed','Cancelled') AND expected_delivery_date<CURRENT_DATE")).rows[0].count;if(prices)await NotificationsService.notifyAdmins("Supplier prices expiring",`${prices} supplier prices expire in the next 12 hours.`,"warning","/suppliers");if(overdue)await NotificationsService.notifyAdmins("Payments overdue",`${overdue} finance payments are overdue.`,"warning","/finance");if(deliveries)await NotificationsService.notifyAdmins("Orders delayed",`${deliveries} orders are past expected delivery.`,"warning","/orders")}catch(e){console.error("Operational alerts failed:",e.message)}}
export function startSchedulers(){if(started)return;started=true;cron.schedule("0 9 * * 1",runWeekly,{timezone:"Asia/Kolkata"});cron.schedule("0 8 * * *",operationalAlerts,{timezone:"Asia/Kolkata"});console.log("Schedulers active: weekly report Monday 09:00 IST; alerts daily 08:00 IST.")}
